import { EthereumTestnetProvider } from './provider';
export declare type FixtureCreator<TFixture, TProvider extends EthereumTestnetProvider = any> = (provider: TProvider) => Promise<TFixture>;
export interface Snapshot<TFixture> {
    data: TFixture;
    id: string;
}
export declare class Snapshots<TProvider extends EthereumTestnetProvider = EthereumTestnetProvider> {
    private readonly provider;
    private readonly snapshots;
    constructor(provider: TProvider);
    snapshot<TFixture>(create: FixtureCreator<TFixture, TProvider>, id?: string): Promise<TFixture>;
    private record;
    private revert;
    toJSON(): {};
}
