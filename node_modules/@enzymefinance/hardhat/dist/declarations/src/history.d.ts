/// <reference types="node" />
import { Contract } from '@enzymefinance/ethers';
export interface EvmMessage {
    to: Buffer;
    data: Buffer;
}
export declare class History {
    private history;
    constructor(seed?: Map<string, string[]>);
    clone(): History;
    override(history: History): void;
    clear(): void;
    reset(contract: Contract | string): boolean;
    calls(contract: Contract | string): string[];
    record(message: EvmMessage): void;
}
