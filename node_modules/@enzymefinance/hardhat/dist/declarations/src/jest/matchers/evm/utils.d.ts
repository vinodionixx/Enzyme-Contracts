/// <reference types="jest" />
import { Contract, ContractFunction } from '@enzymefinance/ethers';
import { utils } from 'ethers';
import { History } from '../../../history';
export declare type MatcherCallback<TReturn extends jest.CustomMatcherResult | Promise<jest.CustomMatcherResult>> = (history: History, contract: Contract, fragment?: utils.FunctionFragment) => TReturn;
export declare function ensureParameters<TSubject extends Contract | ContractFunction<any> = any, TReturn extends jest.CustomMatcherResult | Promise<jest.CustomMatcherResult> = jest.CustomMatcherResult>(subject: TSubject, invert: boolean, callback: MatcherCallback<TReturn>): TReturn;
