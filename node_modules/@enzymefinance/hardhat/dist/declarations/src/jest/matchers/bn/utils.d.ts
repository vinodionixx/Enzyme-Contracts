/// <reference types="jest" />
import { BigNumber } from 'ethers';
export declare type MatcherCallback = (received: BigNumber, expected: BigNumber) => jest.CustomMatcherResult;
export declare function ensureBigNumbers(received: any, expected: any, invert: boolean, callback: MatcherCallback): jest.CustomMatcherResult;
