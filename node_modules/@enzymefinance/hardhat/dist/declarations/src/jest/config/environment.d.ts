import type { EthereumProvider } from 'hardhat/types';
import { HardhatRuntimeEnvironment } from 'hardhat/types';
import NodeEnvironment from 'jest-environment-node';
export interface HardhatTestOptions {
    history: boolean;
    coverage: boolean;
}
export default class EnzymeHardhatEnvironment extends NodeEnvironment {
    private metadataFilePath;
    private tempDir;
    private codeCoverageRuntimeRecording;
    private recordCodeCoverage;
    private recordCallHistory;
    private runtimeEnvironment;
    private removeCallHistoryListener?;
    private removeCodeCoverageListener?;
    constructor(config: any);
    setup(): Promise<void>;
    teardown(): Promise<void>;
}
export declare function getRuntimeEnvironment(): HardhatRuntimeEnvironment;
export declare function addListener(provider: EthereumProvider, event: string, handler: (...args: any) => void): () => void;
